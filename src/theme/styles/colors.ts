import { ThemeColors } from '../elements/colors';
import { IColorStyles } from '../interfaces/styles';

export const ColorStyles: IColorStyles = {
	Default: {
		backgroundColor: ThemeColors.background,
		color: ThemeColors.text,
	},
};
